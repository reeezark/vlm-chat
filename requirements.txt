# VLM智能图文问答助手依赖列表

# 核心依赖
gradio>=5.0,<6.0           # Web界面框架（锁定主版本，避免 6.x API 破坏兼容）
openai>=1.0,<2.0           # OpenAI兼容API客户端
fastapi>=0.115,<1.0        # 健康检查与 Gradio 挂载
uvicorn>=0.30,<1.0         # ASGI 服务运行器

# 可选依赖（用于图片处理）
pillow>=10.0,<12.0         # 图片处理库
pypdf>=4.0,<6.0            # PDF 文档文本提取（多图/文档输入）

# 测试和评测依赖
pytest>=7.0,<9.0           # 测试框架
pytest-cov>=4.0,<6.0       # 测试覆盖率工具
jieba>=0.42.1             # 中文分词（评测脚本使用）

# 其他工具
python-dotenv>=1.0.0       # 环境变量管理
requests>=2.28,<3.0        # HTTP请求库
